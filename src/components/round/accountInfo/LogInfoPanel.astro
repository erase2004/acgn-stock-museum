---
import LogList from './preact/LogList'

type Props = {
  round: string
  userId: string
}

const { round, userId } = Astro.props
---

<div class="panel-content max-h-screen">
  <LogList client:visible round={round} userId={userId} />
</div>
