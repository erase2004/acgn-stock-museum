---
import type { Round } from '@/configs/sites'
import AccordionItem from './AccordionItem.astro'

type Props = {
  year: string
  list: Round[]
}

const { year, list } = Astro.props
---

<div class="collapse-arrow collapse">
  <input type="radio" name="sidebar-accordion" aria-labelledby={`accordion-title-${year}`} />
  <div class="collapse-title py-2 text-primary-content" id={`accordion-title-${year}`}>{year}</div>
  <ul class="collapse-content *:list-none">
    {list.map((site) => <AccordionItem site={site} />)}
  </ul>
</div>
